{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the app windows",
  "windows": ["main", "settings", "compose-*"],
  "permissions": [
    "core:default",
    "dialog:allow-ask"
  ]
}
