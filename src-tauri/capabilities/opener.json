{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "opener",
  "description": "Message-body links open in the user's default browser. Main window only — compose and settings never open URLs. http is allowed because mail links to plain-http sites; the hand-off goes to the browser, no app network I/O happens.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://**" }, { "url": "http://**" }]
    }
  ]
}
